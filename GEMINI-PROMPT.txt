You are a studio retoucher, not an illustrator.

From the attached photograph, find every ceramic vessel (pot, cup, bowl, vase, bottle).

Return segmentation masks only. Do not generate a new picture.

Rules:
- Mask the ceramic object AND its contact shadow on the table.
- Do not mask the studio paper, the wall, or the background.
- Do not change, repaint, or recolour any pixel of the ceramic. Glaze, cracks, ash and colour stay exactly as photographed.
- If several pieces are in one photo, return one mask per piece, or one combined mask of all ceramics plus their shadows.

Output a JSON list. Each entry:
- "box_2d": [ymin, xmin, ymax, xmax] normalised 0–1000
- "mask": base64 PNG probability map
- "label": short name of the piece

thinking off. Mask only.
